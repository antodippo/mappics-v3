# Copy to .env.local for local overrides.
# In production this is set in the Firebase Hosting / CI environment.
# When empty the Vite dev-server proxy rewrites /api/* → http://localhost:8080.
VITE_API_BASE_URL=
